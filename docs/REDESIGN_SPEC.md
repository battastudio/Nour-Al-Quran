# REDESIGN_SPEC — placeholder

The original master prompt (`CLAUDE.md`) references a behavioural spec at this path
(sections C.1–C.18, D.6, D.8). That document was **not provided**. Per the build
decision, screens are implemented from:

1. `CLAUDE.md` (behaviour + hard rules + §6 honesty corrections),
2. the Stitch HTML export in `design/stitch/` (visual truth), and
3. `design/stitch/**/DESIGN.md` (design system).

Per-screen assumptions made in the absence of this spec are noted in code comments
and commit messages. Replace this file with the real spec to tighten fidelity.
