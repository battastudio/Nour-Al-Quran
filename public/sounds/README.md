# Adhan sounds

Place `adhan_makkah.mp3` here (public-domain / CC recording). It is used by:
- Tier D in-app adhan (`src/notifications/inApp.ts`)
- Android native channel sound — also copy it to `android/app/src/main/res/raw/adhan_makkah.mp3`

TODO(asset): add the licensed/public-domain file and record its source in docs/LICENSES.md.
